# 项目实施计划与路线图

> 版本：v0.5 ｜ Stage 0~5 已完成；Stage 6 已完成桌面部署与扩展模型进程/venv 隔离，Qwen3 官方 CPU Adapter 已接入，进入真实 heavy gate 与弱设备收尾

```text
Stage 0       Stage 1        Stage 2       Stage 3     Stage 4      Stage 5       Stage 6
Bootstrap -> Core Runtime -> Adapters -> API       -> Arena UI -> Benchmark -> Hardening
   ✅             ✅            ✅          ✅           ✅          ✅          🟡
```

## WBS

| Stage | 任务 | 状态 |
|---|---|---|
| S0 | 工程骨架 / config / Dummy / CI | ✅ |
| S1 | Adapter / Registry / Metrics / ProcessRunner / ResourceGuard | ✅ |
| S2 | Piper / Kokoro / Qwen3 初始 placeholder | ✅ |
| S3 | FastAPI / benchmark / download / export / streaming | ✅ |
| S4 | Gradio Arena / capability UI / Blind AB | ✅ |
| S5 | TC-01~05 / environment / ZIP / repeats / aggregate / API+CLI+UI Suite | ✅ |
| S6.1 | Windows/macOS/Linux CI smoke + local deployment doctor | ✅ |
| S6.2 | concurrent runner hardening / 压力报告隔离 | 🟡 CPU/内存预算 + pressure profile 已实现，继续真实设备校准 |
| S6.3 | timeout/OOM/watchdog cleanup | ✅ hard timeout / crash 恢复 + worker diagnostics + external protocol handshake |
| S6.4 | CosyVoice/MeloTTS | ✅ real CPU gate + 独立 Python worker + 本地资产/Doctor；仍默认 experimental/disabled |
| S6.5 | Qwen3-TTS 0.6B CustomVoice | 🟡 官方 qwen-tts CPU Adapter + worker + snapshot manifest + manual heavy gate 已实现；真实 CPU gate/量化待完成 |
| S6.6 | ARM/低核弱设备验证 | 🟡 原生 ARM64 + 1-CPU hosted runner CI 已通过；真实目标设备仍待验证 |
| S6.7 | 主 Arena 本地部署验收 | ✅ Python 3.10/3.11/3.12 + Windows/macOS/Linux + UI Doctor + Piper/Kokoro gate 已通过 |

## 本地部署 Gate

主 Arena 已满足本地部署测试条件：

1. Python 3.10/3.11/3.12 主测试矩阵持续验证。
2. Windows、macOS、Linux 均通过 `edgetts-arena doctor`、Dummy WAV、FastAPI health/preset 与 spawn worker smoke。
3. UI job 通过 `edgetts-arena doctor --ui` 与 Gradio mounted smoke。
4. Piper/Kokoro 真实 CPU synthesis gate 通过，可作为桌面本地 Arena 的首批真实模型。
5. CosyVoice/MeloTTS 独立真实 CPU gate 通过，并可通过专用 Python/venv worker 接入主链路。
6. Qwen3 不作为主 Arena ready 的前置条件；其官方 Adapter/worker 路径已接入，但 real heavy gate 仍待单独验收。

## Stage 6 当前优先顺序

1. ✅ 跨平台启动、Doctor、Dummy/API/UI smoke。
2. ✅ benchmark worker / hard timeout / protocol diagnostics。
3. ✅ MeloTTS/CosyVoice real CPU gate 与 external worker。
4. ✅ Qwen3-TTS 0.6B CustomVoice 官方 CPU Adapter、合同测试、本地 snapshot manifest、manual gate。
5. 🟡 实际执行 Qwen3 heavy CPU synthesis gate，记录内存、RTF 与 WAV sanity。
6. 🟡 Qwen3 quantization / low-memory CPU route。
7. 🟡 Concurrent 模式资源预算用真实设备压测校准。
8. 🟡 真实 ARM/弱算力设备验证。

## Stage Gate

每阶段必须：pytest 通过、README/docs 同步、接口破坏显式记录、可选依赖独立 gate、真实模型不影响 Dummy/API baseline。**配置了 gate 不等于 gate 已通过**；只有 Actions/目标机真实 synthesis 成功后才可标记 verified。

## Stage 6 Watchdog 与 Worker 当前口径

- `BenchmarkService` / `RepeatedBenchmarkService` 默认启用进程隔离。
- `keep_in_memory=false` 且未配置外部 Python：使用当前解释器的 `spawn` worker。
- `keep_in_memory=false` 且配置 `worker_python`/`worker_python_env`：使用版本化 JSON stdin/stdout external worker。
- 默认扩展模型环境变量：`EDGETTS_ARENA_QWEN3_PYTHON`、`EDGETTS_ARENA_COSYVOICE_PYTHON`、`EDGETTS_ARENA_MELOTTS_PYTHON`。
- `edgetts-arena doctor --workers` 对配置中声明的专用解释器做协议 + Dummy WAV probe；不代替真实模型 synthesis gate。
- `/api/v1/system/models` 暴露 `worker_mode=in_process|spawn|external` 与 `worker_python_configured`，不回传本机 Python 绝对路径。
- Standard Suite 一个 `case/model` 作为一个 worker group，在 worker 内完成 warm-up + repeats。
- timeout → `3001`；worker crash → `3002`；显式 MemoryError → `2002`；SIGKILL 仅记 `oom_suspected=true`。
- GitHub-hosted 原生 ARM64 与 1-CPU runner 已通过；ResourceGuard 使用 affinity/cgroup/host 中最保守资源预算。
- MeloTTS real CPU gate 与 CosyVoice 300M SFT real CPU gate 已实际通过。
- Qwen3 使用官方 `qwen-tts`、本地 `Qwen3-TTS-12Hz-0.6B-CustomVoice`、CPU fp32 保守基线；manual heavy gate 已存在，但尚未标记成功。
- 下一步仍需 Qwen3 real gate、量化/低内存路线和真实弱算力目标设备验证。
