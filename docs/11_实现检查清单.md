# 实现检查清单

## Stage 0~5
- [x] Bootstrap / Core Runtime / Piper+Kokoro / API / Arena+Blind / Repeated Benchmark Suite

## Stage 6
- [x] Concurrent CPU 线程公平预算与内存前置 gate
- [x] Windows/Linux/macOS Dummy + API smoke CI 已配置
- [x] concurrent/sequential 通过 `execution_profile=pressure|baseline` 明确分离
- [ ] benchmark 主链路接入进程级 timeout/watchdog
- [ ] OOM / worker crash 可恢复
- [ ] 第二批模型不破坏 MVP baseline
- [ ] ARM/弱算力设备验证

## 文档同步规则

API/schema、capability、status enum、benchmark metric、preset、配置字段、UI 规则、artifact schema、Stage 状态改变时必须同步文档。
