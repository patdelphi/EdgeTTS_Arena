# 实现检查清单

## Stage 0~5
- [x] Bootstrap / config / Dummy / CI
- [x] Adapter / Registry / Metrics / ProcessRunner / ResourceGuard
- [x] Piper / Kokoro real CPU
- [x] FastAPI / benchmark / download / export / streaming
- [x] Gradio Arena / capability UI / Blind AB
- [x] TC-01~05 / warm-up / repeats / aggregate / environment / ZIP / API+CLI+UI Suite

## Stage 6
- [x] Concurrent CPU/内存预算与 pressure profile
- [x] Windows/Linux/macOS Doctor + smoke
- [x] Python 3.10/3.11/3.12 主矩阵
- [x] ARM64 hosted + 1-CPU hosted
- [x] process watchdog / hard timeout / crash cleanup / OOM suspected
- [x] worker PID / exit / signal / elapsed / mode / runtime/protocol diagnostics
- [x] external protocol version handshake + bounded diagnostics
- [x] CosyVoice/MeloTTS contract + real CPU gates
- [x] CosyVoice offline WeText local assets
- [x] external Python/venv worker routing in BenchmarkService + Suite
- [x] `worker_python` / `worker_python_env`
- [x] Qwen3/CosyVoice/MeloTTS 默认 worker env
- [x] `doctor --workers` dedicated worker protocol/WAV probe
- [x] `doctor --worker MODEL_ID` 定向 worker probe
- [x] `/system/models` worker status without local interpreter path
- [x] Qwen3-TTS 0.6B CustomVoice official qwen-tts CPU Adapter
- [x] Qwen3 reject Base/1.7B + speaker/language/capability contract tests
- [x] Qwen3 local snapshot + SHA-256 manifest prep
- [x] Qwen3 real-model smoke runner
- [x] Qwen3 dedicated explicit-trigger real CPU gate workflow
- [x] Qwen3 real CPU synthesis gate 实际运行通过（Ubuntu x86_64 / Python 3.11 / qwen-tts 0.1.1 / 2 threads / FP32）
- [x] Qwen3 gate WAV sanity + JSON metrics artifact
- [ ] Qwen3 quantization / low-memory optimized CPU route
- [ ] 真实 ARM/弱算力目标设备验证

## Qwen3 FP32 gate 记录

GitHub Actions run `33761262528`：9.04s audio / 48.05s inference / RTF 5.315 / peak RSS 5317.8MB / RSS delta 862.3MB / 24kHz WAV。功能 gate 通过，但性能数据支持继续量化优化，不将 FP32 视为最终端侧路线。

## 本地部署 Ready 判定

主 Arena 已满足本地部署测试工程判定。CosyVoice/MeloTTS/Qwen3 均已有 hosted real CPU synthesis 证据；三者仍默认 `experimental + disabled`，其中 Qwen3 下一阶段重点是 quantization / low-memory，真实弱设备也仍待验证。

## 文档同步规则

API/schema、capability、status、benchmark metric、preset、配置字段、CLI/UI 规则、artifact schema、Stage 状态改变时必须同步文档。
