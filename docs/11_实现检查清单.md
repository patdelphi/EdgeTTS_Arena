# 实现检查清单

> 用于每个 Stage 完成后的快速自检。

## Stage 0

- [x] `pip install -e .` / CI 安装成功
- [x] `pytest` 无模型环境通过
- [x] Dummy Adapter 可生成音频
- [x] config / logging / errors 有统一入口

## Stage 1

- [x] Adapter capability 契约固定
- [x] ModelRegistry 支持 load/status/unload
- [x] MetricsCollector 不依赖具体 Adapter
- [x] 非流式模型 TTFB 为 `None`
- [x] timeout 可回收 worker
- [x] ResourceGuard 可配置

## Stage 2

- [x] Piper Adapter contract tests
- [x] Piper real CPU smoke
- [x] Kokoro Adapter contract tests
- [x] Kokoro int8 real CPU smoke
- [x] Qwen3 experimental/unavailable placeholder
- [x] 不伪造 runtime/capability/audio

## Stage 3

- [x] FastAPI app factory + Pydantic v2 schemas + `/healthz`
- [x] `GET /api/v1/system/models`
- [x] `POST /api/v1/benchmark/run` 同步最终结果
- [x] 单模型失败不影响其他模型
- [x] WAV + `benchmark_report.json` + `environment.json`
- [x] audio download / ZIP export
- [x] download/export 防路径穿越
- [x] WebSocket streaming capability gate
- [x] 流式首块真实 TTFB；非流式 `ttfb=null`
- [x] `edgetts-arena serve`
- [x] Stage 3 CI：44 tests + Piper/Kokoro real CPU 全绿

## Stage 4

- [x] Gradio 作为可选 `[ui]` extra，不污染基础 API 安装
- [x] `edgetts-arena serve --ui`，Arena 挂载 `/arena/`
- [x] UI 与 FastAPI 共享同一 Registry / BenchmarkService / artifact store
- [x] 1~4 模型选择；2~4 作为横向 Arena 推荐模式
- [x] UI 参数由 capability 驱动
- [x] 多模型 voice 自动退回各模型默认值
- [x] Sequential 默认；Concurrent 仍受 ResourceGuard 约束
- [x] 4 个音频/结果卡 + metrics comparison table
- [x] `ttfb=null` 显示 `N/A (non-streaming)`
- [x] 单模型 error 卡不影响其他卡片
- [x] ZIP export
- [x] Blind AB 匿名随机顺序
- [x] Naturalness / Intelligibility / Prosody 1~5
- [x] 全部匿名 sample 评分完成前禁止 Reveal
- [x] Reveal 后保存匿名 mapping + ratings + run_id
- [x] `blind_scores.json` 写入 run 并进入 ZIP
- [x] 独立 `ui-smoke` CI 验证 Blocks 构建和 FastAPI mount

## Stage 5

- [ ] TC-01~05 preset 可执行
- [ ] warm-up 策略固定
- [ ] repeated benchmark 可配置
- [ ] median / P95 / variance 等汇总
- [ ] environment snapshot 字段复核
- [ ] report 含 model/runtime/quantization/version
- [ ] ZIP 可完整复现输入、输出与 Blind 评分

## Stage 6

- [ ] Windows/Linux/macOS smoke test
- [ ] concurrent 与 sequential 压测报告分开
- [ ] memory/timeout/OOM 故障可恢复
- [ ] 第二批模型不破坏 MVP baseline

## 文档同步规则

每次改变以下内容时必须同步文档：

- API path/schema
- Adapter capability
- Model status enum
- Benchmark metric definition
- 配置字段
- UI capability rule
- Blind artifact schema
- Stage 状态
