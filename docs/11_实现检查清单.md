# 实现检查清单

> 用于每个 Stage 完成后的快速自检。

## Stage 0

- [x] `pip install -e .` 成功（正式 CI 覆盖 Python 3.10~3.12）
- [x] `pytest` 无模型环境通过
- [x] Dummy Adapter 可生成音频
- [x] config / logging / errors 有统一入口

## Stage 1

- [x] Adapter capability 契约固定
- [x] ModelRegistry 支持 load/status/unload
- [x] MetricsCollector 不依赖具体 Adapter
- [x] 非流式模型 TTFB 为 `None`
- [x] timeout 可回收 worker
- [x] ResourceGuard 可配置

## Stage 2

- [x] Piper Adapter contract tests（fake runtime，无模型 CI 也可跑）
- [x] Piper 实机 CPU 生成成功（`piper-model-smoke` GitHub Actions）
- [x] Kokoro Adapter contract tests（当前直接文本 en-us/en-gb）
- [x] Kokoro 实机 CPU 生成成功（`kokoro-model-smoke` GitHub Actions）
- [x] 每个 Stage 2 Adapter / placeholder 有 contract tests
- [x] Qwen3 保持 experimental/unavailable，不伪造 runtime/capability/audio

## Stage 3

- [x] FastAPI app factory + Pydantic v2 schemas + `/healthz`
- [x] `GET /api/v1/system/models`
- [x] `POST /api/v1/benchmark/run` 同步返回最终结果
- [x] 单模型失败不影响其他模型
- [x] benchmark 生成 WAV + `benchmark_report.json` + `environment.json`
- [x] `GET /api/v1/audio/download/{run_id}/{filename}`
- [x] `GET /api/v1/export/{run_id}`
- [x] download/export 无路径穿越
- [x] WebSocket streaming capability gate 生效
- [x] 流式首块报告 TTFB；非流式 benchmark 仍为 `ttfb=null`
- [x] CLI `edgetts-arena serve` 可启动本地 API

## Stage 4

- [ ] UI 参数由 capability 驱动
- [ ] Sequential 默认
- [ ] `ttfb=null` 显示 N/A
- [ ] Blind 匿名映射可追溯到 run_id

## Stage 5

- [ ] TC-01~05 可执行
- [ ] environment snapshot 完整
- [ ] report 含 model/runtime/quantization/version
- [ ] ZIP 可复现本轮输入和输出

## Stage 6

- [ ] Windows/Linux/macOS smoke test
- [ ] concurrent 与 sequential 报告分开
- [ ] memory/timeout 故障可恢复
- [ ] 第二批模型不破坏 MVP baseline

## 文档同步规则

每次改变以下内容时必须同步文档：

- API path/schema
- Adapter capability
- Model status enum
- Benchmark metric definition
- 配置字段
- Stage 状态
