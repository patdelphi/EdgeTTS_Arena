# 实现检查清单

## Stage 0~5
- [x] Bootstrap / Core / Adapters / API / Gradio / Benchmark Suite

## Stage 6
- [x] 跨平台 Doctor / Python 3.10~3.12 / ARM64 hosted / 1-CPU hosted
- [x] ResourceGuard / pressure profile / watchdog / worker diagnostics
- [x] CosyVoice/MeloTTS hosted real CPU + external workers
- [x] Qwen3 official 0.6B CustomVoice FP32 real CPU gate
- [x] Qwen3 native pure-C Adapter + pinned manifest
- [x] native subprocess RSS/CPU metrics
- [x] INT8@2 / INT8@4 / INT4@4 hosted quant comparison
- [x] INT8@2 当前推荐 CPU 候选
- [x] `config.language` / `language_control` / CLI / UI
- [x] INT4 独立 disabled/experimental catalog entry
- [x] `prepare_qwen3_native_variants.py` 成对 manifest 准备
- [x] 多模型共同 voice/language intersection control
- [x] Blind AB 可固定同 text/voice/language/seed 比较 INT8 vs INT4
- [x] `target_device_acceptance.py` 真实设备多次 synthesis + environment + aggregate + threshold + ZIP 验收包
- [x] native 真机验收显式检查 `OPENBLAS_NUM_THREADS` 未预设
- [x] `bootstrap_extended_model.py` Qwen3/MeloTTS/CosyVoice pinned venv/runtime/assets/Doctor planner + explicit executor
- [x] bootstrap 默认 plan-only；只有 `--execute` 才执行联网重安装/下载
- [x] bootstrap 生成 `bootstrap_plan.json` + `env.sh` + `env.ps1`
- [x] MeloTTS `prepare_melotts_assets.py`：NLTK + pinned model + descriptor + SHA-256 manifest
- [x] CosyVoice CPU requirements helper + pinned 300M SFT model helper
- [x] Qwen3 standalone model prep 默认固定 Arena 已验证 revision
- [x] MeloTTS/CosyVoice heavy gate 复用本地 preparation helper，减少本地/CI 漂移
- [ ] 实际执行 Qwen3 INT8/INT4 人工 Blind AB 并形成质量结论
- [ ] 在真实 ARM/弱算力目标设备运行验收包并形成设备结论

## 本地部署 Ready

**主 Arena 已满足本地部署测试工程判定，现在可以开始本地部署测试。** 该判定基于跨平台 Doctor、Python 3.10~3.12、UI/API/spawn smoke、Piper/Kokoro 真实 CPU gate 等持续 CI 证据；扩展模型仍默认 experimental/disabled。

扩展模型 bootstrap 现在属于工程 ready：可以生成与 heavy gate 对齐的专用环境并做 Worker Doctor，但 **bootstrap 成功仍不等于真实模型 synthesis gate 或目标设备性能 gate 已通过**。

## 边界

量化性能 route、Blind AB harness、目标设备验收工具和扩展模型 bootstrap 都已完成，但：

- harness ready 不等于 quality verified；
- target-device harness ready 不等于真实硬件已经 verified；
- hosted ARM64/1-CPU smoke 不等于树莓派/目标设备性能结论；
- bootstrap/Doctor ready 不等于对应重模型已经在该本机完成真实 synthesis。

只有真实人工评分、真实模型 smoke 与真实设备报告产生后，才分别升级对应 gate 状态。
